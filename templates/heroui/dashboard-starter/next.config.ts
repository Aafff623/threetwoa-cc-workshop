import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // HeroUI Pro 组件可能需要特定的 transpile 配置
  // 如需启用，取消下方注释：
  // transpilePackages: ["@heroui/react", "@heroui-pro/react"],
};

export default nextConfig;
